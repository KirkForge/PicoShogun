# Prometheus scrape config for Shogun
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "shogun"
    metrics_path: /metrics
    static_configs:
      - targets: ["shogun:8765"]
    basic_auth:
      username: admin
      password: changeme
